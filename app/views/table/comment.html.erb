<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>ANOTHER</title>
</head>
<body>
  <div class="table">
    <table border="1">
      <tr>
        <th>ID</th><th>contents</th><th>post_id</th><th>user_id</th><th>post.created_at</th><th>post.updated_at</th></tr>
        <% @comments.each do |comment| %>
        <tr>
          <td><%= comment.id %></td>
          <td><%= comment.content %></td>
          <td><%= comment.post_id %></td>
          <td><%= comment.user_id %></td>
          <td><%= comment.created_at %></td>
          <td><%= comment.updated_at %></td>
        </tr>
        <% end %>
      </div>
    </body>
    </html>
